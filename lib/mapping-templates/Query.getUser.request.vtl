#if($ctx.info.selectionSetList.contains("ownedImage"))
    #set($limit = 11)
#else
    #set($limit = 1)
#end

{
    "version": "2018-05-29",
    "operation": "Query",
    "query": {
        "expression": "PK = :pk",
        "expressionValues": {
            ":pk": $util.dynamodb.toDynamoDBJson("USER#$ctx.args.userId")
        }
    },
    "limit": $limit,
    "scanIndexForward": false
}